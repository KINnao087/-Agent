server:
  address: 0.0.0.0
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/contract_review?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:zjj2005225}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 200MB

# JWT 配置
app:
  jwt:
    secret: Y29udHJhY3QtcmV2aWV3LXBsYXRmb3JtLWp3dC1zZWNyZXQta2V5LTIwMjU=
    expiration-ms: 86400000  # 24 小时

# Python AI 服务地址
python:
  service:
    url: http://${PYTHON_HOST:localhost}:${PYTHON_PORT:8000}

logging:
  level:
    com.contract: DEBUG
    org.springframework.security: DEBUG
